apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafanadata
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: slow
